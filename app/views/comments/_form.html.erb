<%= form_with model: [ @article, Comment.new ] do |form| %>

  <div class="field">
    <%= form.text_area :body, style:"width: 100%; height: 80px;" %>
  </div>

  <div class="field">
    <%= form.submit %>
  </div>
  
<% end %>